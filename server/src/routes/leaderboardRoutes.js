const express = require('express');
const router = express.Router();

// Temporary route until you implement the full functionality
router.get('/', (req, res) => {
  res.json([]);
});

module.exports = router;